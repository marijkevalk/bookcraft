target-version = "py312"
line-length = 88

[lint]
select = ["E", "F", "I", "B", "UP", "SIM", "RUF"]

[lint.per-file-ignores]
"tests/**" = ["E501"]
# webui.py embeds an HTML/CSS template string; long lines there are expected.
"src/bookcraft/webui.py" = ["E501"]
